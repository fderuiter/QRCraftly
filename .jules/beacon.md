## 2024-05-15 - Missing html lang attribute\n**Discovery:** The root HTML tag was missing a `lang` attribute, leaving the primary language undefined.\n**Signal:** Configure the global Vike config in `src/pages/+config.ts` to include `lang: 'en'`.\n**Impact:** Ensures crawlers and screen readers know the content language, a fundamental SEO best practice.\n**Verification:** Verified via `pnpm build` and `pnpm test` that the attribute is present without breaking builds.
